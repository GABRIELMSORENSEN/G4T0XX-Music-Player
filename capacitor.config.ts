import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.g4t0xx.musicplayer',
  appName: 'g4t0xx-music-player',
  webDir: 'dist/public'
};

export default config;
